# 开发环境说明

## 推荐环境

| 项目 | 推荐值 |
| --- | --- |
| 系统 | Ubuntu 24.04 |
| ROS2 | Jazzy |
| 构建工具 | colcon |
| 默认控制器 IP | `192.168.10.21` |
| 默认命名空间 | `/robot1` |

## 第一次使用

进入工作空间：

```bash
cd /home/simon/lbot_ws
```

加载 ROS2：

```bash
source /opt/ros/jazzy/setup.bash
```

安装动态库：

```bash
cd /home/simon/lbot_ws/src/lbot_driver/lib
sudo ./lib_install.sh
```

编译：

```bash
cd /home/simon/lbot_ws
colcon build
source install/setup.bash
```

启动驱动：

```bash
ros2 launch lbot_driver lbot_start_driver.launch.py
```

## 网络配置

机器人控制器默认 IP 是：

```text
192.168.10.21
```

参赛电脑建议设置到同一网段，例如：

```text
192.168.10.100
255.255.255.0
```

检查是否连通：

```bash
ping 192.168.10.21
```

Web 控制平台：

```text
http://192.168.10.21:8000
```

固件升级页面：

```text
http://192.168.10.21:5000
```

## 修改机器人 IP

配置文件：

```text
src/lbot_driver/config/lbot_config.yaml
```

默认内容：

```yaml
lbot_driver:
  ros__parameters:
    arm_ip: "192.168.10.21"
```

启动文件里也有机器人列表：

```text
src/lbot_driver/launch/lbot_start_driver.launch.py
```

默认：

```python
robots = [
    {"name": "robot1", "arm_ip": "192.168.10.21"},
]
```

如果现场 IP 改了，优先确认 launch 文件中的 IP 正确。

## 运行示例程序

```bash
cd /home/simon/lbot_ws
source install/setup.bash
ros2 launch lbot_demo lbot_start_demo.launch.py
```

示例 launch 一次只启动一个 demo。要切换示例，修改：

```text
src/lbot_demo/launch/lbot_start_demo.launch.py
```

把想运行的 `executable` 取消注释即可。

## 常见示例

| 示例 | 作用 |
| --- | --- |
| `demo_lkls_movej` | 左臂关节运动 |
| `demo_lkrs_movej` | 右臂关节运动 |
| `demo_lkls_movel` | 左臂直线运动 |
| `demo_lkrs_movel` | 右臂直线运动 |
| `demo_lkls_i1_movej` | 双臂 I1 场景关节运动，当前 demo launch 默认启动 |
| `demo_lkls_i1_movel` | 双臂 I1 场景直线运动 |
| `demo_lkls_i1_movejp` | 双臂 I1 场景末端位姿运动 |
| `demo_fk` | 正运动学 |
| `demo_ik` | 逆运动学 |
| `demo_tool_frame` | 工具坐标系 |
| `demo_hand_l6_control` | L6/O6 灵巧手控制 |
| `demo_hand_l10_control` | L10 灵巧手控制 |
| `demo_emergency_enable` | 急停和使能 |

如果不确定先跑哪个示例，建议先从 `demo_lkls_i1_movej` 或 `demo_lkls_movej` 开始，并把速度、加速度保持在较小值。

## 遥操作

遥操作默认读取 `can0`，波特率 `1000000`：

```bash
ros2 launch lbot_teleop lbot_start_teleop.launch.py
```

启动顺序建议：

1. 先启动 `lbot_driver`。
2. 确认左右臂 MoveJ 服务存在。
3. 再启动 `lbot_teleop`。

如果没有遥操作设备或 `can0` 没配置，可以先跳过这一部分，不影响普通 ROS2 服务调用开发。
