# LinkerBot 开发交接规范

本文是后续开发必须遵守的仓库约定。目标是在增加比赛功能时，持续保持目录稳定、代码
可读、组件可复用，并避免业务逻辑与 ROS 或某一台开发机绑定。

## 不可破坏的目录边界

三个目录必须位于同一父目录下：

```text
├── linkerbot_ws/       # 本 Git 仓库
├── orbbec_ws/          # 静态外部相机依赖
└── Dexterous-Hand/     # 静态外部机器人依赖
```

- 日常功能开发只修改 `linkerbot_ws`。
- `orbbec_ws` 和 `Dexterous-Hand` 视为只读依赖；运行、构建和测试脚本不得自动拉取、
  更新或修改它们。
- 外部依赖版本变化必须单独验证，并更新 `docs/provenance.md`。
- 不得把外部仓库复制进 `linkerbot_ws/src`，也不得提交它们的构建产物。

## 各目录职责

- `apps/`：应用入口和进程生命周期编排，不放识别、规划或控制算法。
- `config/`：所有用户可调参数的唯一来源，按功能分目录维护。
- `linkerbot/`：可复用的 Python 适配层，负责路径、配置和底层进程命令。
- `scripts/`：交付给使用者的稳定入口；每个完整功能都必须有对应脚本。
- `src/`：本仓库维护的 ROS package 和 C++ 代码。
- `tools/`：外参求解、数据检查等独立工具，不依赖比赛任务状态机。
- `tests/`：配置、纯算法和入口契约测试。
- `artifacts/`：标定结果、采集数据和运行日志，不作为源码目录。
- `docs/`：架构决策、接口说明和外部依赖来源。

新文件必须按职责放置，不得为了方便在仓库根目录、ROS package 或脚本中散落配置、
日志和临时代码。

## 依赖方向

依赖只能沿以下方向流动：

```text
scripts -> apps -> linkerbot 适配层 -> ROS 节点/外部 SDK
                            \-> 可复用算法模块
```

- 纯算法优先接收普通 C++/Python 数据结构，不直接接收 ROS message，也不自行订阅 topic。
- ROS subscriber、publisher、service client 和 message 转换集中在命名明确的 adapter/node。
- 任务状态机依赖抽象接口，不直接创建相机或机械臂 ROS client。
- `apps/` 只组合组件；禁止把算法复制到多个入口文件。
- 出现第二个调用者时，必须把共享逻辑提取到 `linkerbot/` 或独立算法库，禁止复制粘贴。
- 不允许反向依赖，例如算法模块导入 `apps/`，或工具依赖具体任务控制器。

## 配置规则

- 可调整的话题名、坐标系、设备 profile、阈值、超时、文件位置和安全开关都写入
  `config/`，不得硬编码在 Bash 或应用入口中。
- 同一参数只能有一个权威配置来源。ROS package 内不得再维护一份 `config/` 或 launch
  参数副本。
- 仓库内文件和数据目录使用相对仓库根目录的路径；系统安装前缀可以显式配置，但禁止
  提交个人用户名、绝对主目录或其他机器特定路径。
- 新配置必须有注释、合理默认值、加载时校验和自动化测试。
- 本地差异使用被 Git 忽略的 `config/*.local.env`，不得直接改成个人路径后提交。
- 任何能够触发真实机械臂运动的配置，默认执行开关必须为 `false`。

## 标定边界

- Gemini 2 的彩色/深度内参、畸变参数和两传感器内部外参以固件及 Orbbec 驱动发布值为
  唯一来源；禁止提交另一份 `camera_info.yaml`、棋盘内参工具或 `color_info_url` 覆盖。
- 使用原始图像的代码必须同时处理 `CameraInfo.K`、`D` 和 `distortion_model`。公共转换
  统一复用 `lbot_vision::CameraGeometry`，不得在节点中重新手写只使用 `K` 的反投影。
- 相机到机器人、相机到末端、TCP 和现场基准属于外参。其配置只放在
  `config/calibration/extrinsics/`，求解工具只放在 `tools/extrinsic_calibration/`，产物只写入
  `artifacts/calibration/extrinsics/`。
- 尚未测量的外参不得用单位矩阵、零向量或经验数值冒充有效标定，也不得发布到正式 TF。

## 用户入口与 ROS 封装

- 使用者只运行 `scripts/*.sh`，不要求记忆 package、节点名、`ros2 run` 或参数拼接方式。
- 每个脚本从自身位置推导仓库根目录，不能假设当前工作目录或安装路径。
- ROS 命令只允许出现在内部适配层；不要在多个脚本中重复拼接命令。
- Python 入口使用 `argparse`，错误信息必须指出缺失的文件或配置项。
- 多进程应用必须正确转发 `SIGINT`/`SIGTERM`，并在退出时回收所有子进程。
- 新入口应提供无硬件检查方式，例如 `--dry-run`、配置测试或录包输入。
- 已公开的脚本名称和参数视为稳定接口；需要改名时保留兼容入口并写迁移说明。

## ROS package 规则

- `CMakeLists.txt` 和 `package.xml` 必须显式声明实际使用的依赖，删除无用依赖。
- 节点只负责一种职责；感知节点不得发送机械臂运动指令。
- topic、service、frame 和 QoS 等外部接口需要在配置或接口文档中说明。
- 节点输出优先使用结构化消息；临时 JSON/status 文本不得成为跨模块的长期核心协议。
- 坐标数据必须携带明确的 frame；TF 缺失时不得把相机坐标冒充机器人基座坐标。
- 实验代码放入 `experimental/` 且默认不参与构建，不得从正式入口调用。

## 可读性与复用性

- 文件、类和函数使用描述职责的名称，避免 `test2`、`new_node`、`utils2` 等临时命名。
- 函数保持单一职责；公共行为写简短 docstring/注释，注释说明“为什么”而非复述代码。
- 优先小型清晰接口，避免全局可变状态、隐藏副作用和跨目录相对导入技巧。
- 错误不得静默忽略；日志应包含组件、失败对象以及使用者可以采取的下一步。
- 不得提交注释掉的大段旧实现。仍有参考价值但不安全的原型应明确隔离并写明限制。
- 运行生成内容只能进入 `artifacts/`、`build/`、`install/` 或 `log/`。

## 机械臂安全要求

任何新的真实运动功能在进入正式入口前，至少具备：

- 默认关闭的执行开关和明显的仿真/真实设备模式；
- 急停、使能状态、关节状态、TF 和通信超时检查；
- 关节限位、速度/加速度限制以及工作空间边界检查；
- 失败后停止或回到安全状态的确定行为；
- 录包或仿真测试，以及在低速条件下完成的真实设备验证记录。

未经测量的占位位姿不得连接真实机械臂运行。

## 标准开发流程

1. 先阅读 `README.md`、`docs/architecture.md` 和本文件，确认新功能所属层级。
2. 先定义配置和模块接口，再实现算法或 ROS adapter。
3. 为新行为添加测试，并提供一个 `scripts/` 用户入口。
4. 执行：

   ```bash
   ./scripts/test.sh
   ./scripts/build.sh
   git status --short
   ```

5. 检查没有绝对个人路径、重复配置、意外生成物或外部仓库改动。
6. 在提交说明中记录测试环境；涉及硬件时注明设备、固件、profile 和测试时长。

## 完成定义

一个功能只有在满足以下条件时才算完成：

- 放在正确目录且符合依赖方向；
- 参数集中在 `config/` 并经过校验；
- ROS/SDK 细节没有泄漏到业务算法和用户操作中；
- 有稳定脚本入口、清晰失败信息和退出清理逻辑；
- 测试与低并发完整构建通过；
- 没有降低机械臂安全默认值；
- README 或相关文档足以让下一位开发者复现使用方法。
