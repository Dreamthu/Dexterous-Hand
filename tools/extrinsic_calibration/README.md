# 外参工具预留

这里是未来外参采集、求解和验证工具的边界。目前尚未确定相机安装方式、标定目标和
机器人接口，因此暂不提供可能误用的占位程序。

工具应从 `config/calibration/extrinsics/` 读取明确配置，把结果写入
`artifacts/calibration/extrinsics/`，并输出可复核的误差报告。相机固件已经提供的内参、
畸变和彩色—深度内部外参不得在这里重新估计或覆盖。
