# 相机与视觉传感器说明

## 本次相机信息

| 项目 | 内容 |
| --- | --- |
| 相机型号 | Orbbec Gemini2 |
| Python SDK | `pyorbbecsdk` |
| ROS2 SDK | `OrbbecSDK_ROS2`，使用 `v2-main` 分支 |
| 推荐 ROS2 启动文件 | `gemini2.launch.py` |
| 用途 | RGB 图像、深度图、点云、目标定位、手眼标定 |

官方资料：

- Python SDK：https://gitee.com/orbbecdeveloper/pyorbbecsdk/tree/main
- ROS2 SDK：https://github.com/orbbec/OrbbecSDK_ROS2/tree/v2-main

## 建议使用方式

比赛程序建议优先使用 ROS2 SDK，因为它能直接发布图像、深度、点云、内参和 TF，方便和机械臂 ROS2 服务连接。

Python SDK 更适合：

- 单独测试 Gemini2 是否能打开。
- 快速保存 RGB/深度图。
- 写简单视觉验证脚本。
- 不需要 ROS2 时做离线调试。

## ROS2 SDK 安装思路

当前 `lbot_ws/src` 只提供机器人 ROS2 SDK，不包含 Orbbec 相机 ROS2 包。如果现场没有安装 Orbbec ROS2 包，可以源码安装：

```bash
mkdir -p ~/orbbec_ws/src
cd ~/orbbec_ws/src
git clone https://github.com/orbbec/OrbbecSDK_ROS2.git
cd OrbbecSDK_ROS2
git checkout v2-main

cd ~/orbbec_ws
source /opt/ros/jazzy/setup.bash
colcon build --event-handlers console_direct+ --cmake-args -DCMAKE_BUILD_TYPE=Release
source install/setup.bash
```

Linux 下需要安装 udev 规则，否则普通用户可能打不开相机：

```bash
cd ~/orbbec_ws/src/OrbbecSDK_ROS2/orbbec_camera/scripts
sudo bash install_udev_rules.sh
sudo udevadm control --reload-rules
sudo udevadm trigger
```

## 启动 Gemini2

```bash
source ~/orbbec_ws/install/setup.bash
ros2 launch orbbec_camera gemini2.launch.py
```

常用参数示例：

```bash
ros2 launch orbbec_camera gemini2.launch.py \
  camera_name:=camera \
  color_width:=640 color_height:=360 color_fps:=15 color_format:=MJPG \
  depth_width:=640 depth_height:=400 depth_fps:=15 depth_format:=Y14 \
  ir_width:=640 ir_height:=400 ir_fps:=15 ir_format:=Y8 \
  enable_color:=true \
  enable_depth:=true \
  depth_registration:=true \
  enable_point_cloud:=true \
  enable_colored_point_cloud:=false \
  publish_tf:=true
```

| 参数 | 作用 |
| --- | --- |
| `camera_name` | 相机命名空间，默认一般为 `camera` |
| `enable_color` | 是否发布彩色图 |
| `enable_depth` | 是否发布深度图 |
| `depth_registration` | 深度和彩色是否对齐 |
| `enable_point_cloud` | 是否发布点云 |
| `enable_colored_point_cloud` | 是否发布彩色点云 |
| `publish_tf` | 是否发布相机内部 TF |
| `color_info_url` | 可指定彩色相机内参文件 |
| `ir_info_url` | 可指定 IR/深度相关内参文件 |
| `enable_publish_extrinsic` | 是否发布相机内部外参信息 |

## 常见话题

实际话题名以 `ros2 topic list` 为准，通常会有以下几类：

| 话题 | 类型 | 说明 |
| --- | --- | --- |
| `/camera/color/image_raw` | `sensor_msgs/msg/Image` | 彩色图 |
| `/camera/color/camera_info` | `sensor_msgs/msg/CameraInfo` | 彩色相机内参 |
| `/camera/depth/image_raw` 或 `/camera/depth/image_rect_raw` | `sensor_msgs/msg/Image` | 深度图 |
| `/camera/depth/camera_info` | `sensor_msgs/msg/CameraInfo` | 深度相机内参 |
| `/camera/depth/color/points` | `sensor_msgs/msg/PointCloud2` | 点云 |
| `/tf`、`/tf_static` | `tf2_msgs/msg/TFMessage` | 相机坐标系 |

检查命令：

```bash
ros2 topic list
ros2 topic echo /camera/depth/camera_info --once
```

## 内参说明

内参主要来自 `CameraInfo`：

| 字段 | 含义 |
| --- | --- |
| `width` / `height` | 图像分辨率 |
| `k` | 3x3 相机矩阵 |
| `d` | 畸变参数 |
| `distortion_model` | 畸变模型 |
| `p` | 投影矩阵 |

建议保存一份现场内参：

```bash
mkdir -p /home/simon/lbot_ws/开发资源/calibration
ros2 topic echo /camera/color/camera_info --once > /home/simon/lbot_ws/开发资源/calibration/gemini2_color_camera_info.yaml
ros2 topic echo /camera/depth/camera_info --once > /home/simon/lbot_ws/开发资源/calibration/gemini2_depth_camera_info.yaml
```

如果已经提供了真实内参文件，学生应优先使用现场提供的文件，不要用文档里的示例值替代真实标定结果。

如果修改分辨率、对齐模式、相机固件或驱动参数，内参可能变化，需要重新保存。

## 外参说明

外参指相机坐标系和机器人坐标系之间的关系。比赛中建议统一使用机器人模型里的 base：

```text
base_torso_root
```

固定相机推荐坐标关系：

```text
base_torso_root -> camera_link -> camera_color_optical_frame
```

手眼相机推荐坐标关系：

```text
arm_left_L8_Link -> camera_link -> camera_color_optical_frame
```

外参建议保存为：

```text
/home/simon/lbot_ws/开发资源/calibration/gemini2_extrinsics.yaml
```

格式示例：

```yaml
parent_frame: base_torso_root
child_frame: camera_color_optical_frame
translation: [0.0, 0.0, 0.0]
rotation_xyzw: [0.0, 0.0, 0.0, 1.0]
note: "示例值，比赛现场需替换为真实标定结果"
```

这里的 `translation` 和 `rotation_xyzw` 只是格式示例。实际抓取程序必须使用现场标定得到的真实外参。

发布静态 TF 示例：

```bash
ros2 run tf2_ros static_transform_publisher \
  --x 0.3 --y 0.0 --z 0.8 \
  --qx 0 --qy 0 --qz 0 --qw 1 \
  --frame-id base_torso_root \
  --child-frame-id camera_color_optical_frame
```

## 和机械臂怎么配合

视觉抓取一般流程：

1. Gemini2 输出 RGB 图、深度图或点云。
2. 视觉算法识别物体，得到物体在相机坐标系下的位置。
3. 用 TF 把点转换到 `base_torso_root`。
4. 规划预抓点、抓取点、撤离点。
5. 调用机械臂 `move_pose` 或 `move_linear` 服务。
6. 调用 O6 灵巧手话题完成抓取或释放。

机械臂调用示例：

```bash
ros2 service call /robot1/left_arm/move_linear lbot_arm_interfaces/srv/MoveL \
"{position: {x: 0.3, y: 0.1, z: -0.2}, euler: {x: 0.0, y: -1.57, z: 0.0}, speed: 0.2, acce: 0.2, block: true}"
```

## 调试检查

| 检查项 | 命令/方法 |
| --- | --- |
| 相机是否识别 | `ros2 run orbbec_camera list_devices_node` |
| 是否有图像 | `ros2 topic list` 后查看 image 话题 |
| 是否有内参 | `ros2 topic echo /camera/depth/camera_info --once` |
| 是否有点云 | 查看 `/camera/depth/color/points` 或实际 points 话题 |
| TF 是否正常 | `ros2 run tf2_tools view_frames` |
| 机械臂是否可控 | 查看 `/robot1/left_arm/move_linear` 服务 |

## 常见问题

| 问题 | 可能原因 | 处理 |
| --- | --- | --- |
| 相机打不开 | udev 规则未安装或 USB 权限不足 | 安装 udev 规则，重新插拔相机 |
| 没有点云 | `enable_point_cloud` 未开启 | 启动时设置 `enable_point_cloud:=true` |
| RGB 和深度对不上 | 未开启对齐 | 设置 `depth_registration:=true` |
| 抓取位置偏 | 外参不准或相机移动 | 重新做手眼标定 |
| 深度跳动 | 反光、透明、黑色物体或距离不合适 | 调整光照、角度、距离 |

## 保存建议

建议保存到：

```text
/home/simon/lbot_ws/开发资源/calibration/
├── gemini2_color_camera_info.yaml
├── gemini2_depth_camera_info.yaml
└── gemini2_extrinsics.yaml
```

相机位置、支架、分辨率、对齐模式改变后，都要重新确认内参和外参。

## 黑框螺母识别节点

工作区中的 `lbot_vision` 提供一个只做视觉、不驱动机械臂的识别节点。它适用于相机固定在工作区上方并斜向下拍摄的布置：通过颜色分割定位黑框和蓝色螺母筐，在黑框内用圆形/边缘检测找到三颗螺母，按外接半径排序为 `large`、`medium`、`small`，并用深度和相机内参输出三维坐标。

启动相机后，在另一个终端执行：

```bash
cd /home/leilll/lbot_ws
source /opt/ros/jazzy/setup.bash
source install/setup.bash
source orbbec_ws/install2/setup.bash
ros2 launch lbot_vision nut_detector.launch.py
```

输出话题：

| 话题 | 类型 | 内容 |
| --- | --- | --- |
| `/nut_detections` | `geometry_msgs/msg/PoseArray` | 按大、中、小顺序排列的三颗螺母位置 |
| `/nut_slots` | `geometry_msgs/msg/PoseArray` | 蓝色螺母筐三个格子的中心，按图像长边方向排列 |
| `/nut_detections/status` | `std_msgs/msg/String` | JSON 状态、像素坐标、三维坐标和坐标系 |
| `/nut_detection/debug_image` | `sensor_msgs/msg/Image` | 黑框、螺母筐、螺母和格子标注图 |

检查结果：

```bash
ros2 topic echo /nut_detections/status
ros2 topic echo /nut_detections --once
ros2 topic echo /nut_slots --once
```

只有当 `/nut_detections/status` 报告 `"status":"ok"` 时才应进入抓取流程。若没有外部手眼标定 TF，节点会在状态中说明并使用相机坐标系发布；不能把该坐标直接用于机械臂运动。现有 `nut_task.launch.py` 仍包含示例性的运动流程和占位位姿，调试识别时不要启动它。
