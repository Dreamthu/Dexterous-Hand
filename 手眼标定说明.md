# 手眼标定说明

## 手眼标定是干什么的

相机看到的点在相机坐标系里，机械臂运动需要机器人坐标系里的点。手眼标定就是求这两个坐标系之间的变换。

## 两种安装方式

| 类型 | 相机位置 | 常见用途 |
| --- | --- | --- |
| Eye-to-Hand | 相机固定在桌子或支架上 | 看整个工作区 |
| Eye-in-Hand | 相机装在机械臂末端 | 近距离抓取 |

## 推荐流程

1. 固定相机，不要晃动。
2. 标定相机内参。
3. 准备棋盘格、ArUco 或 AprilTag 标定板。
4. 让机械臂到多个姿态，记录末端位姿。
5. 同时记录标定板在相机里的位姿。
6. 计算相机坐标系和机器人坐标系的关系。
7. 用几个已知点验证误差。

## 建议保存的文件

```text
开发资源/calibration/
├── gemini2_color_camera_info.yaml
├── gemini2_depth_camera_info.yaml
├── gemini2_extrinsics.yaml
└── test_points.txt
```

其中 `gemini2_color_camera_info.yaml` 和 `gemini2_depth_camera_info.yaml` 保存相机内参，`gemini2_extrinsics.yaml` 保存相机到机器人坐标系的外参。

## 外参格式建议

```yaml
parent_frame: base_link
child_frame: camera_color_optical_frame
translation: [0.0, 0.0, 0.0]
rotation_xyzw: [0.0, 0.0, 0.0, 1.0]
```

实际数值以现场标定结果为准。

## 发布静态 TF 示例

```bash
ros2 run tf2_ros static_transform_publisher \
  --x 0.3 --y 0.0 --z 0.8 \
  --qx 0 --qy 0 --qz 0 --qw 1 \
  --frame-id base_link \
  --child-frame-id camera_color_optical_frame
```

## 和机械臂动作怎么接

视觉程序输出目标点后，一般这样做：

1. 把相机坐标系点转换到 `base_link`。
2. 加一个安全高度，得到预抓点。
3. 用 `move_pose` 或 `move_linear` 运动到目标附近。
4. 闭合灵巧手。
5. 抬起或撤离。

## 注意

- 相机移动后必须重新标定。
- 夹具换了以后要重新确认工具坐标系。
- 视觉点不要直接拿来抓，最好先加预抓点和安全高度。
